# 🌐 Prompt: Playwright E2E Flow

**Context**
Playwright spec files live under `e2e/web/`.
Follow `.clinerules/testing.md`.

**Scenario**
Describe flow here → login, navigate to dashboard, create item, logout.

**Expect**
* Use `@playwright/test`.
* Selectors via `getByRole` or data-testids.
* Add `test.step` annotations.

**Output**
Create the test file at appropriate path and with right code with full spec path comment on top.

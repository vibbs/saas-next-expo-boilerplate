# ✅ Prompt: Add Unit Test

**Context**
We use **Vitest** + **Testing Library**.
Test files co-locate with source and end in `.test.tsx`.

**Task**
Write a unit test for `<filePath>` covering:
1. Nominal behavior.
2. One edge case.
3. Snapshot (if UI).

**Output**
Create the test file at appropriate path and with right content.
